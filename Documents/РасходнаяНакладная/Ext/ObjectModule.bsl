Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	СуммаДокумента = 0;
	КолТов = Товары.Количество();
	Если КолТов Тогда
		СуммаДокумента = СуммаДокумента + Товары.Итог("Всего");
	КонецЕсли;
	КолУсл = Услуги.Количество();
	Если КолУсл Тогда
		СуммаДокумента = СуммаДокумента + Услуги.Итог("Всего");
	КонецЕсли;
КонецПроцедуры
